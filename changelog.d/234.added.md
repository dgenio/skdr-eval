`EvaluationArtifact.rows()` / `.row(model, estimator)` return typed `ReportRow` objects (a thin view over `artifact.report`) so downstream code can read results by attribute instead of DataFrame column strings.
