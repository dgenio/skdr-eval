`EvaluationArtifact.compare()` / `skdr_eval.compare_artifacts()` and a `skdr-eval compare` CLI command diff two evaluation runs (per-`(model, estimator)` value deltas, support-health transitions, warning-code changes, verdict flips) and gate CI on verdict regressions.
