All public evaluators now route every user-supplied frame through the single `coerce_to_pandas` seam (`skdr_eval._frames`): `evaluate_pairwise_models` also coerces its `external_policies` tables and `simulate_autoscaling_scenario` coerces its `logs_df`/`op_daily_df`, so polars / pyarrow inputs are accepted uniformly across the API rather than only on the standard path. This is a pure input-handling change — no estimator, propensity, or uncertainty semantics are affected.
