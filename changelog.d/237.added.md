`EvaluationArtifact.to_markdown()` and `export(formats=["markdown"])` emit a compact, paste-ready Markdown summary (V̂, CI, and the support-health/ESS/Pareto-k trust diagnostics) for PRs, tickets, and chat.
