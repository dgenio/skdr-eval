The moving-block bootstrap (`block_bootstrap_ci`, used by `ci_bootstrap=True`) now draws each replicate from an independent `numpy.random.SeedSequence` child instead of one shared `RandomState` stream. This makes the interval reproducible under any `n_jobs`; exact CI endpoints differ from prior releases for the same `random_state` (the point estimates and coverage are unchanged).
