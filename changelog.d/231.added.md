`skdr-eval evaluate` / `pairwise` gain `--format {table,csv,json,markdown}`: the headline report goes to stdout in the chosen format and the "wrote N files" confirmation moves to stderr, so the CLI composes in shell pipelines. Omitting `--format` keeps the previous output.
