Published, versioned JSON Schema for the artifact and card: `ArtifactSchema.json_schema()`, committed files under `docs/schemas/`, and a `skdr-eval schema --kind artifact|card` command so downstream tooling can validate `skdr-eval` output without importing the library.
