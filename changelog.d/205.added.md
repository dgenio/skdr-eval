Published a versioned JSON Schema for the artifact payload: `ArtifactSchema.json_schema()`, a committed `docs/schemas/artifact.schema.json`, and a `skdr-eval schema --kind artifact` command, so downstream tooling can validate `skdr-eval` output without importing the library. (The card/verdict schema is deferred until the experiment-eligibility contract stabilizes.)
