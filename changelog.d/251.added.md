`EvaluationArtifact.badge()` and a `skdr-eval badge` command generate a dependency-free SVG + Markdown "evaluation badge" whose colour tracks `support_health`, so a thin-support result reads as cautionary rather than oversold.
