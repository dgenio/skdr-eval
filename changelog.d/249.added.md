`EvaluationArtifact.to_summary_facts()` emits a compact, LLM-ready facts payload; a provider-agnostic prompt template (`docs/recipes/llm-summary-prompt.md`) turns it into a natural-language executive summary with no runtime LLM dependency.
