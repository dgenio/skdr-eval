Added regression guards pinning two existing performance invariants of the standard evaluator: policy induction issues a single vectorised `predict` call (#209), and the propensity/outcome nuisances are fit once and shared across the whole DR estimator family — only MRDR adds its required sample-weighted outcome refit (#248).
